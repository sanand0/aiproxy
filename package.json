{
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "lint": "npx -y prettier@3.5 --print-width 120 '**/*.{js,md}' --write && npx -y oxlint --fix"
  },
  "prettier": {
    "printWidth": 120
  },
  "devDependencies": {
    "wrangler": "^4.27.0"
  },
  "dependencies": {
    "jose": "^5.4.1"
  }
}
